keeperhubplan.md

# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
.pytest_cache/

# ML training artifacts
runs/
*.ckpt
*.pt
# ...except the distilled gnubg core: a versioned build input that
# sample_trainer.gnubg_published_core_init loads to seed every minted MLP.
!agent/data/gnubg_core.pt

# Node / Next.js
node_modules/
.next/
out/
build/
.pnpm-store/

# Hardhat
contracts/artifacts/
contracts/cache/
contracts/deployments/localhost/
contracts/deployments/hardhat*.json
contracts/typechain-types/

# Per-agent server-managed wallet keystores (Phase 60). Never commit
# these — each file is an encrypted v3 keystore for an EOA holding real
# (testnet) ETH; even encrypted, leaking them weakens the agent-wallet
# threat model.
server/data/

# Misc
.DS_Store
*.log

# LibreOffice / OpenOffice editor lock files
.~lock.*#

docs/video/
docs/boards/

.claude/worktrees